
{
    "$id": "${{siteId}}",
    "$schema": "https://schema.yext.com/config/pages/yextsite-config/v1",
    "domainConfig": "${{domainName}}",
    "siteName": "${{siteName}}",
    "repoConfig": "${{repoId}}"
  }
  